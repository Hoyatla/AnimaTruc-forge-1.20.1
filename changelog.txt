v1.0.0-proprietary.10 (AnimaTruc)
- Add clean-room gameplay-animation runtime package:
  - `io.hoyatla.animatruc.core.gameplay`
  - typed gameplay events, actor state, runtime state, module registry, animation intents, camera feedback, HUD signals
- Add built-in gameplay modules inspired by common movement/action mods without copying third-party code:
  - locomotion and stamina actions
  - emote intent handling
  - sound perception / alertness
  - combat feedback
  - explosion feedback
  - inventory weight / fatigue
  - input/UI movement intents
- Add Forge gameplay config:
  - `config/animatruc-gameplay.toml`
  - feature toggles and runtime tuning values
- Extend the Forge config screen from preset-only controls to `AnimaTruc Runtime`:
  - gameplay toggles
  - existing limb preset toggles
- Add Forge server/client integration:
  - explosion event dispatch on server side
  - feedback packet from server to client
  - client camera shake/roll and HUD overlay feedback
  - periodic inventory weight estimation
- Add gameplay runtime tests for locomotion, perception, explosion feedback, weight handling, and disabled-feature fallback.
- Keep the implementation proprietary and clean-room:
  - no copied gameplay classes
  - no third-party assets
  - modular runtime intended for future Minecraft-specific adapters.

v1.0.0-proprietary.9 (AnimaTruc)
- Split licensing for publication:
  - `animatruc-proprietary-core`, `animatruc-forge`, and `animatruc-geckolib-compat` are `All Rights Reserved`
- Refactor `animatruc-geckolib-compat` into a distinct bridge addon:
  - bridge mod id changed to `animatruc_geckobridge`
  - explicit dependencies now include `geckolib` official (external)
  - no `software.bernie.geckolib` classes shipped in bridge jar
- Add Gecko bridge runtime pipeline:
  - `GeckoResourceLocator`
  - `GeckoAssetImporter`
  - `GeckoAssetCache`
  - `GeckoClipResolver`
  - non-crashing fallback on lookup/parse/clip resolution failures
- Add resource reload cache invalidation for bridge imports.
- Add module-local legal files and notices for compatibility publishing:
  - `animatruc-geckolib-compat/LICENSE`
  - `animatruc-geckolib-compat/THIRD_PARTY_NOTICES.txt`
- Update compat packaging so legal files are embedded in runtime jar, sources jar, and javadoc jar.
- Add minimal core runtime extension points for bridge consumers:
  - `AnimationClipResolverRegistry`
  - `AnimaTrucRuntime.resolveExternalClip(...)`
  - `AnimaTrucRuntime.createExternalClipState(...)`
- Clarify mixed licensing and bridge architecture in documentation.

v1.0.0-proprietary.8 (AnimaTruc)
- Add new Forge module `animatruc-geckolib-compat` producing:
  - `compat-animatruc-geckolib-1.20.1.jar`
- Compatibility module exposes `modId=geckolib` and a GeckoLib API shim subset:
  - `GeoAnimatable`, `AnimatableInstanceCache`, `AnimationController`, `AnimatableManager`, `RawAnimation`
  - `GeoModel`, `Defaulted*GeoModel`, `GeoEntityRenderer`, `GeoRenderLayer`, utility aliases
- Add bridge runtime from Gecko-compatible controller requests to AnimaTruc animator instances.
- Add dedicated compat config (`animatruc-geckolib-compat-client.toml`) with:
  - compat enable switch
  - strict API mode
  - max compat animators
  - performance profile
- Expand GeckoLib compatibility surface for external mods:
  - add animatable contracts (`GeoItem`, `GeoBlockEntity`, `SingletonGeoAnimatable`, stateless variants)
  - add renderer shims (`GeoItemRenderer`, `GeoArmorRenderer`, `GeoBlockRenderer`, `GeoObjectRenderer`, `GeoReplacedEntityRenderer`, `DynamicGeoEntityRenderer`)
  - add keyframe/molang/object/network/event compatibility types used by runtime linking
  - align packet constructor/method signatures with GeckoLib Forge 1.20.1 patterns (`encode/decode/receivePacket`)
- Add symbol coverage audit against PrismLauncher instance mods:
  - report file: `compat_geckolib_symbol_report_after.json`
  - detected symbols: 205
  - provided symbols: 226
  - unresolved symbols: 0
- Important packaging mode:
  - this compatibility jar is designed as a GeckoLib replacement layer and should not be loaded alongside official GeckoLib.

v1.0.0-proprietary.7 (AnimaTruc)
- Add automatic locomotion presets in core runtime:
  - biped (2 legs), hexapod (6 legs), octopod (8 legs), myriapod (many legs)
  - confidence-scored skeleton detection with anti-false-positive filters
  - chain-level metadata (side, anchor, lengths, gait phase)
- Add Forge preset builder:
  - `AutoGroundContactPresetFactory` to generate IK chains + ground-contact limb setup
  - `AutoGroundContactPreset` bundle with direct `createIkModifier()` and `createGroundController()`
- Add Forge client config system:
  - `animatruc-client.toml` with profile toggles and performance modes
  - in-game config menu screen (`AnimaTruc Presets`) for lightweight machines
- Add tests for auto-detection reliability (biped, hexapod, myriapod trimming).

v1.0.0-proprietary.6 (AnimaTruc)
- Add multi-limb IK runtime overlay for foot/leg chains:
  - new `MultiLimbIKModifier` with per-chain target keys
  - new `IkChainDefinition` and `IkPlane` to configure humanoid/spider limbs
  - optional per-chain scalar weight parameter for blend control
- Extend `AnimatorContext` for vector parameters:
  - `vectorParameters` map and `vector(key, fallback)` accessor
  - builder helpers for scalar/vector single-parameter writes
- Add Forge ground-contact utilities for Minecraft block terrain:
  - `GroundContactController`: per-limb stance/swing state + downward block raycasts
  - `GroundContactLimbDefinition`: limb anchors and stepping configuration
  - `GroundContactContextAdapter`: bridge to inject IK targets into animator context
- Add tests covering context vector parameters and multi-limb IK behavior.

v1.0.0-proprietary.5 (AnimaTruc)
- Add mesh geometry support in `animatruc-pack`:
  - new core asset types: `ModelMesh`, `ModelMeshFace`, `ModelUv`
  - `ModelGeometry` now carries both cubes and meshes
- Upgrade AnimaTruc JSON importer to parse:
  - `model.meshes[].vertices`
  - `model.meshes[].faces` (indices + UVs)
- Upgrade Blockbench plugin exporter to include mesh payload when project uses Mesh objects.
- Keep `.animatrucpack` runtime format unchanged while extending geometry coverage.
- Add regression assertions for mesh-only pack imports.

v1.0.0-proprietary.4 (AnimaTruc)
- Add unified `animatruc-pack` workflow (model + animations in one runtime pack file).
- Add model geometry payload in core assets:
  - `ModelGeometry`
  - `ModelCube`
  - `AnimationAssetPack` now carries geometry + skeleton + clips
- Add importer route upgrades:
  - new extension support: `.animatrucpack`, `.animatrucpack.json`
  - source-name based importer entrypoint (`importFromString`) for runtime/resource loading
- Upgrade Blockbench plugin to V2 (`blockbench-plugin/animatruc_exporter.js`):
  - native open/import shortcuts for `.bbmodel` and `.gltf`
  - project validation action
  - pack preview action
  - unified runtime pack export (`.animatrucpack.json`)
- Add Forge runtime loader + example bootstrap:
  - `AnimaTrucForgePackLoader`
  - `AnimaTrucExampleBootstrap`
  - shipped example pack resource under `assets/animatruc/animatrucpacks`
- Add E2E test proof:
  - plugin-exported pack fixture imported through runtime importer with assertions

v1.0.0-proprietary.3 (AnimaTruc)
- Add dedicated AnimaTruc JSON importer (`.animatruc.json`, `.animatruc`) in core runtime.
- Add direct import route in `AnimationAssetImporters` for AnimaTruc pack files.
- Add Blockbench plugin in repository:
  - `blockbench-plugin/animatruc_exporter.js`
  - export action for direct AnimaTruc pack generation
  - validation action for channels/bones consistency
- Add importer regression tests for AnimaTruc pack format and route resolution.
- Add Blockbench plugin usage guide in `blockbench-plugin/README.md`.

v1.0.0-proprietary.2 (AnimaTruc)
- Add asset model import API with stable pack output (`AnimationAssetPack`).
- Add skeleton representation (`ModelSkeleton`, `ModelBone`) for authoring pipeline.
- Add Blockbench importer:
  - parse outliner/bones hierarchy
  - parse animation channels (position/rotation/scale)
  - convert keyframes to AnimaTruc runtime clips
- Add glTF JSON importer:
  - parse nodes and parent hierarchy
  - parse animation samplers/channels
  - decode FLOAT accessors from embedded or external buffers
  - convert translation/rotation/scale tracks to AnimaTruc clips
- Add extension-based importer facade (`AnimationAssetImporters`) for `.bbmodel` and `.gltf`.
- Add regression tests for bbmodel import, gltf import, and importer routing.

v1.0.0-proprietary.1 (AnimaTruc)
- Add layered animation system with per-layer weighting and per-bone masks.
- Add animation graph runtime:
  - immutable graph definition builder
  - state transitions with conditions and priorities
  - cross-fade windows between active states
- Add procedural modifier pipeline:
  - look-at rotation modifier
  - breathing micro-motion modifier
  - two-bone IK modifier (planar XY)
- Add runtime profiler for graph/mix/modifier/total frame timings.
- Extend animator context to carry look targets and scalar parameters.
- Extend math and transform primitives for procedural runtime use.
- Add regression tests for graph transitions, layer masking, and modifiers.

v1.0.0-proprietary.0 (AnimaTruc)
- Remove inherited legacy source modules from this repository.
- Keep only clean-room runtime packages under `io.hoyatla.animatruc.*`.
- Add proprietary runtime module `animatruc-proprietary-core`:
  - keyframe channels
  - deterministic mixer (weighted + additive)
  - adaptive update scheduler
  - per-instance animator runtime
- Add clean Forge integration module `animatruc-forge` for Minecraft 1.20.1.
- Replace repository license with strict "All Rights Reserved".
